/*function attack(weapon, beast, startHealth) {
    alert(`You are in the attack function`);
    alert(`You are carrying a ${weapon.Name}`);
    if (beast.Speed > weapon.Speed) {
        let hit = beast.Damage * (beast.Speed - weapon.Speed);
        let health = startHealth - hit;
        alert(`You've been hit and now have ${health}`);
    }
}*/

attack();
